export const StatusOptions = {
    Success: 'success',
    Failure: 'failure',
  };
  
  export const Status = null; // Used to indicate status, can be `null`, `success`, or `failure`
  
  /**
   * Represents a group of items as a set of strings.
   * @typedef {Set<string>} ItemGroup
   */
  